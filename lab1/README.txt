Project correctly forks off child process and then reads the return of fork() to 
determine if it is the child process or the parent process.
Successfully prints pid, ppid, and cpid of the parent process as well as the status 
of the child.
Successfully prints pid and ppid of the child.
Reads and prints time used by the sytem and user for both the parent and child.
Reads and prints total time of parent process.

9/11 researched unix commands time(), times(), fork() as well as the struct returned by
times() call.
9/12 wrote program in one sitting
9/13 configured make file and pushed everything to git