Program works exactly as intended. It accepts an unsigned long int address in memory as a command line argument and 
computes the page number and offset of that memory address.

11/1 Finished writing the program in a few minutes...